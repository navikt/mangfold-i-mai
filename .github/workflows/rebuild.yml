name: Rebuild

on:
  schedule:
    - cron:  '42 * * * *'

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Call GitHub pages build endpoint
        run: |
          curl \
          -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/navikt/mangfold-i-mai/pages/builds
        env:
          GH_TOKEN: ${{ secrets.REBUILD }}




